///<reference path="./typings/angular.d.ts" />
///<reference path="./typings/jquery.d.ts" />

module directives{
    
    //declare class SixteenSegment {
    //    constructor(numSegments : number, canvas : HTMLElement, width?:number, height?:number);
    //    DispayText(text : string);
    //    FillDark:string;
    //}
    
    //export function SegmentDisplay() : ng.IDirective {
    //    var directive : ng.IDirective = {
    //        scope: {
    //            text: '@'
    //        },
    //        link: (scope : ng.IScope, element : JQuery, attributes) => {
                
    //            //var canvas = $('<canvas></canvas>').appendTo(element);
    //            //canvas.width(element.width());
    //            //canvas.height(element.height());
                
    //            var segment16 = new SixteenSegment(8, element[0]);
    //            segment16.FillDark = '#001100';
                
    //            scope.$watch('text', (newValue, oldValue) =>{
    //                segment16.DispayText(newValue);
    //            });
    //        }
    //    };
        
    //    return directive;
    //}
}